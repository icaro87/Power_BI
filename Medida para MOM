MOM Faturamento = 

--- MONTH OVER MONTH DE FATURAMENTO

VAR MES_ATUAL = [Faturamento]
VAR MES_ANTERIOR = CALCULATE( [Faturamento], DATEADD( d_Calendario[Date], -1, MONTH ) )
VAR MOM = DIVIDE( MES_ATUAL - MES_ANTERIOR, MES_ANTERIOR , 0 )

RETURN MOM
